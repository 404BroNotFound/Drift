# Drift

Drift is a front-end web project built with HTML, CSS, and JavaScript.

## Project structure

- `html/` contains the site pages.
- `css/` contains the stylesheets.
- `js/` contains the JavaScript files.
- `assets/` contains images and other visual assets.

## Run locally

Open `html/index.html` in a web browser.
